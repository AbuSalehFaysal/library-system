<%- include('partials/header') %>
<div class="ui main text container">
  <div class="ui huge header">Welcome to History Blog</div>
  <div class="ui divided items">
    
     
    
    <% blogs.forEach(blog => { %>
    
    <div class="item">
      <div class="image">
        <img src="<%= blog.image %> " alt="" />
      </div>
      <div class="content">
        <a class="header" href="/blogs/<%= blog._id %>"><%= blog.title %></a>
        <a class="header" href="/blogs/<%= blog._id %>"><%= blog.author %></a>
        <a class="header" href="/blogs/<%= blog._id %>"><%= blog.genre %></a>
        <a class="header" href="/blogs/<%= blog._id %>"><%= blog.releasedate %></a>
        <div class="meta">
          <span><%= blog.created.toDateString() %></span>
        </div>
        <div class="description">
          <p><%- blog.body.substring(0,100) %>...</p>
        </div>
        <div class="extra">
          <a class="ui inverted violet button" href="/blogs/<%= blog._id %>"
            >Read More<i class="arrow alternate circle right icon"></i
          ></a>
        </div>
      </div>
    </div>
    
    <% }) %>
    
  </div>
</div>
<%- include('partials/footer') %>
